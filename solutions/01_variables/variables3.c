#include <stdio.h>

int main() {
    int number = 3;
    printf("Number: %d\n", number);

    // TODO: Fix the code to properly compile.
    number = 4;
    printf("Number: %d\n", number);
    return 0;
}